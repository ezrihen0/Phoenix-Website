import { Section } from "@/components/ui/Section";
import { Card } from "@/components/ui/Card";
export function StaticReviews() { const cards = [["Clear Diagnostics", "Homeowners get plain-language findings and next steps."],["Professional Service", "Respectful, clean service with reliable arrival communication."],["Insurance-Ready", "WETT and service reporting prepared for practical decisions."]] as const; return <Section><p className="section-kicker">Trust Signals</p><h2 className="section-title">Trusted by Alberta Homeowners</h2><div className="mt-6 grid gap-4 md:grid-cols-3">{cards.map(([t,b]) => <Card key={t} title={t} body={b} />)}</div></Section>; }
