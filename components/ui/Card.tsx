export function Card({ title, body }: { title: string; body: string }) { return <article className="card-premium p-5"><h3 className="text-lg font-semibold text-[var(--inner-page-title)]">{title}</h3><p className="mt-2 text-sm leading-6 text-[var(--text-secondary)]">{body}</p></article>; }
