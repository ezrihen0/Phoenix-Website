export function ServiceFAQ({ items }: { items: { q: string; a: string }[] }) { return <section className="container-shell py-12"><p className="section-kicker">Common Questions</p><h2 className="section-title">FAQ</h2><div className="mt-4 space-y-3">{items.map((item) => <details key={item.q} className="card-premium p-4"><summary className="cursor-pointer font-medium text-[var(--inner-page-title)]">{item.q}</summary><p className="mt-2 text-[var(--text-secondary)]">{item.a}</p></details>)}</div></section>; }
